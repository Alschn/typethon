let i: int = 0 + 1;
const b: int = 33;
const bl: bool = (33 > 13 + (4 / 2 % 3) < 4) != 4;
let nullable?: int;

def f(arg1: int, arg2: int): int => {
    return arg1 + arg2;
}

while (true) {
    let _: func = (): void => {}
    let o: bool = (not or true) and false;
}

if (nullable == null) o = not true;
